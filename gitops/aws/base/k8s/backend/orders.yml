apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders

spec:
  replicas: 1
  selector:
    matchLabels:
      app: orders
  template:
    metadata:
      labels:
        app: orders
    spec:
      containers:
        - name: orders
          image: 368763425814.dkr.ecr.us-east-1.amazonaws.com/orders:e512498fbee8a57e711edf504bb4d0059ae0ece0
          ports:
            - containerPort: 3005
          env:
            - name: NODE_ENV
              value: development
            - name: PORT
              value: "3005"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: boutique-secrets
                  key: ORDERS_DB_URL
            - name: PRODUCTS_SERVICE_URL
              value: "http://product-service:3003"

---

apiVersion: v1
kind: Service
metadata:
  name: orders

spec:
  selector:
    app: orders
  ports:
    - protocol: TCP
      name: http
      port: 3005
      targetPort: 3005
  type: ClusterIP
