apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-scripts

data:
  10-create-databases.sql: |
    -- Create all required databases
    CREATE DATABASE auth_db;
    CREATE DATABASE products_db;
    CREATE DATABASE orders_db;
    CREATE DATABASE users_db;

  20-init-schema.sql: |
    -- ============================================================
    -- AUTH DB
    -- ============================================================
    \c auth_db

    CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

    CREATE TABLE IF NOT EXISTS users (
        id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
        email VARCHAR(255) UNIQUE NOT NULL,
        password_hash VARCHAR(255) NOT NULL,
        first_name VARCHAR(100),
        last_name VARCHAR(100),
        phone VARCHAR(50),
        address TEXT,
        role VARCHAR(50) DEFAULT 'customer',
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

    -- ============================================================
    -- PRODUCTS DB
    -- ============================================================
    \c products_db

    CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

    CREATE TABLE IF NOT EXISTS categories (
        id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
        name VARCHAR(100) NOT NULL,
        description TEXT,
        image_url VARCHAR(500),
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

    CREATE TABLE IF NOT EXISTS products (
        id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
        name VARCHAR(255) NOT NULL,
        slug VARCHAR(255),
        description TEXT,
        short_description TEXT,
        sku VARCHAR(100),
        brand VARCHAR(100),
        category_id UUID REFERENCES categories(id),
        price DECIMAL(10,2) NOT NULL,
        compare_price DECIMAL(10,2),
        materials TEXT,
        care_instructions TEXT,
        inventory_quantity INTEGER DEFAULT 0,
        is_featured BOOLEAN DEFAULT false,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

    CREATE TABLE IF NOT EXISTS product_images (
        id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
        product_id UUID REFERENCES products(id) ON DELETE CASCADE,
        image_url VARCHAR(500) NOT NULL,
        alt_text VARCHAR(255),
        is_primary BOOLEAN DEFAULT false,
        sort_order INTEGER DEFAULT 1,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

    -- ============================================================
    -- ORDERS DB
    -- ============================================================
    \c orders_db

    CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

    CREATE TABLE IF NOT EXISTS orders (
        id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
        user_id UUID,
        total_amount DECIMAL(10,2) NOT NULL,
        status VARCHAR(50) DEFAULT 'pending',
        shipping_address TEXT,
        payment_status VARCHAR(50) DEFAULT 'pending',
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

    CREATE TABLE IF NOT EXISTS order_items (
        id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
        order_id UUID REFERENCES orders(id) ON DELETE CASCADE,
        product_id UUID,
        quantity INTEGER NOT NULL,
        price DECIMAL(10,2) NOT NULL,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

  30-seed-data.sql: |
    -- ============================================================
    -- SEED: PRODUCTS DB
    -- ============================================================
    \c products_db

    INSERT INTO categories (id, name, description) VALUES
    ('10000000-0000-0000-0000-000000000001', 'Dresses', 'Elegant dresses for special occasions'),
    ('10000000-0000-0000-0000-000000000002', 'Accessories', 'Luxury accessories and fashion items'),
    ('10000000-0000-0000-0000-000000000003', 'Bags', 'Designer handbags and tote bags'),
    ('10000000-0000-0000-0000-000000000004', 'Outerwear', 'Coats and jackets'),
    ('10000000-0000-0000-0000-000000000005', 'Shoes', 'Designer footwear and heels')
    ON CONFLICT DO NOTHING;

    INSERT INTO products (name, slug, description, short_description, sku, brand, category_id, price, compare_price, inventory_quantity, is_featured) VALUES
    ('Silk Evening Gown', 'silk-evening-gown', 'Beautiful floor-length gown crafted from premium silk', 'Luxurious silk evening gown', 'LEG-001', 'LUXE BOUTIQUE', '10000000-0000-0000-0000-000000000001', 1899.00, 2299.00, 15, true),
    ('Cashmere Coat', 'cashmere-coat', 'Elegant wool and cashmere blend coat for winter', 'Warm luxury coat', 'COAT-001', 'LUXE BOUTIQUE', '10000000-0000-0000-0000-000000000004', 899.00, 1200.00, 20, true),
    ('Leather Handbag', 'leather-handbag', 'Premium Italian leather tote bag', 'Luxury leather tote', 'BAG-001', 'LUXE BOUTIQUE', '10000000-0000-0000-0000-000000000003', 599.00, 799.00, 25, true),
    ('Diamond Necklace', 'diamond-necklace', 'Stunning diamond pendant necklace', 'Elegant diamond jewelry', 'JWL-001', 'LUXE BOUTIQUE', '10000000-0000-0000-0000-000000000002', 2999.00, 3999.00, 10, true),
    ('Designer Heels', 'designer-heels', 'Elegant stiletto heels in premium leather', 'Luxury high heels', 'SHOES-001', 'LUXE BOUTIQUE', '10000000-0000-0000-0000-000000000005', 499.00, 699.00, 18, true)
    ON CONFLICT DO NOTHING;
    
    INSERT INTO products (id, name, slug, description, short_description, sku, brand, category_id, price, compare_price, inventory_quantity, is_featured) VALUES 
    (gen_random_uuid(), 'Gucci Sunglasses', 'gucci-sunglasses', 'Oversized luxury sunglasses', 'Luxury sunglasses', 'SUN-001', 'Gucci', '10000000-0000-0000-0000-000000000002', 450.00, 550.00, 30, true), 
    (gen_random_uuid(), 'Prada Loafers', 'prada-loafers', 'Classic leather loafers', 'Leather loafers', 'SHOES-002', 'Prada', '10000000-0000-0000-0000-000000000005', 850.00, 950.00, 20, true), 
    (gen_random_uuid(), 'Louis Vuitton Keepall', 'lv-keepall', 'Iconic monogram duffle bag', 'Classic travel bag', 'BAG-002', 'Louis Vuitton', '10000000-0000-0000-0000-000000000003', 2100.00, 2200.00, 5, true), 
    (gen_random_uuid(), 'Chanel No 5 Perfume', 'chanel-no5', 'Classic floral fragrance', 'Iconic perfume', 'PERF-001', 'Chanel', '10000000-0000-0000-0000-000000000002', 150.00, 160.00, 50, true), 
    (gen_random_uuid(), 'Hermes Silk Tie', 'hermes-silk-tie', 'Printed silk twill tie', 'Luxury silk tie', 'TIE-001', 'Hermes', '10000000-0000-0000-0000-000000000002', 220.00, 240.00, 15, true),
    (gen_random_uuid(), 'Rolex Submariner Watch', 'rolex-submariner', 'Iconic luxury diving watch with Oystersteel bracelet', 'Luxury diving watch', 'WATCH-001', 'Rolex', '10000000-0000-0000-0000-000000000002', 9500.00, 9500.00, 3, true),
    (gen_random_uuid(), 'Cartier Love Bracelet', 'cartier-love', '18K Yellow Gold iconic Love bracelet with screw motif', '18K Gold bracelet', 'JEWEL-001', 'Cartier', '10000000-0000-0000-0000-000000000004', 7350.00, 7350.00, 5, true),
    (gen_random_uuid(), 'Burberry Trench Coat', 'burberry-trench', 'Classic double-breasted cotton gabardine trench coat', 'Classic trench coat', 'COAT-002', 'Burberry', '10000000-0000-0000-0000-000000000001', 2490.00, 2490.00, 15, true),
    (gen_random_uuid(), 'YSL Leather Wallet', 'ysl-wallet', 'Black quilted leather wallet with gold-tone monogram', 'Leather wallet', 'WALLET-001', 'Yves Saint Laurent', '10000000-0000-0000-0000-000000000002', 650.00, 650.00, 20, true),
    (gen_random_uuid(), 'Tom Ford Sunglasses', 'tom-ford-sun', 'Oversized cat-eye tortoiseshell sunglasses', 'Cat-eye sunglasses', 'SUN-002', 'Tom Ford', '10000000-0000-0000-0000-000000000002', 495.00, 495.00, 25, true)
    ON CONFLICT DO NOTHING;

    INSERT INTO product_images (product_id, image_url, alt_text, is_primary, sort_order) 
    SELECT p.id, 'https://images.unsplash.com/photo-1511499767150-a48a237f0083?w=800&q=80', p.name || ' - Main image', true, 1 
    FROM products p WHERE p.sku = 'SUN-001' ON CONFLICT DO NOTHING;

    INSERT INTO product_images (product_id, image_url, alt_text, is_primary, sort_order) 
    SELECT p.id, 'https://images.unsplash.com/photo-1549298916-b41d501d3772?w=800&q=80', p.name || ' - Main image', true, 1 
    FROM products p WHERE p.sku = 'SHOES-002' ON CONFLICT DO NOTHING;

    INSERT INTO product_images (product_id, image_url, alt_text, is_primary, sort_order) 
    SELECT p.id, 'https://images.unsplash.com/photo-1584916201218-f4242ceb4809?w=800&q=80', p.name || ' - Main image', true, 1 
    FROM products p WHERE p.sku = 'PERF-001' ON CONFLICT DO NOTHING;

    INSERT INTO product_images (product_id, image_url, alt_text, is_primary, sort_order) 
    SELECT p.id, 'https://images.unsplash.com/photo-1548036328-c9fa89d128fa?w=800&q=80', p.name || ' - Main image', true, 1 
    FROM products p WHERE p.sku = 'BAG-002' ON CONFLICT DO NOTHING;

    INSERT INTO product_images (product_id, image_url, alt_text, is_primary, sort_order) 
    SELECT p.id, 'https://images.unsplash.com/photo-1595341596012-70b9eb6b1c09?w=800&q=80', p.name || ' - Main image', true, 1 
    FROM products p WHERE p.sku = 'TIE-001' ON CONFLICT DO NOTHING;

    INSERT INTO product_images (product_id, image_url, alt_text, is_primary, sort_order) 
    SELECT p.id, '/product-images/rolex-watch.jpg', p.name || ' - Main image', true, 1 
    FROM products p WHERE p.sku = 'WATCH-001' ON CONFLICT DO NOTHING;

    INSERT INTO product_images (product_id, image_url, alt_text, is_primary, sort_order) 
    SELECT p.id, '/product-images/cartier-bracelet.jpg', p.name || ' - Main image', true, 1 
    FROM products p WHERE p.sku = 'JEWEL-001' ON CONFLICT DO NOTHING;

    INSERT INTO product_images (product_id, image_url, alt_text, is_primary, sort_order) 
    SELECT p.id, '/product-images/trench-coat.jpg', p.name || ' - Main image', true, 1 
    FROM products p WHERE p.sku = 'COAT-002' ON CONFLICT DO NOTHING;

    INSERT INTO product_images (product_id, image_url, alt_text, is_primary, sort_order) 
    SELECT p.id, '/product-images/leather-wallet.jpg', p.name || ' - Main image', true, 1 
    FROM products p WHERE p.sku = 'WALLET-001' ON CONFLICT DO NOTHING;

    INSERT INTO product_images (product_id, image_url, alt_text, is_primary, sort_order) 
    SELECT p.id, '/product-images/sunglasses.jpg', p.name || ' - Main image', true, 1 
    FROM products p WHERE p.sku = 'SUN-002' ON CONFLICT DO NOTHING;
